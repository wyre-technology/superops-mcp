name: superops_clients_list returns is_error when credentials missing
server:
  command: node
  args: [dist/index.js]
assert:
  tool: superops_clients_list
  args: {}
  expect:
    is_error: true
timeout: 30s
